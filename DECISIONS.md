# Decisions Log — Slip Flow (Deploy or Die, Track A)

Running record of every meaningful decision, in order. Continues the same discipline used on the Track B build (`whatswithavi/gdg_deploy_or_die`), which stays untouched as a fallback submission.

## Why this project exists

Started as a Track B submission (AI Code Review Assistant — done, CI green, tagged `v1.0.0`, kept as-is). Mid-build, a fully pre-written Track A plan (OCR raw-material slip digitization, human-in-the-loop approval, `SlipExtractionAgent`/`QueryAgent`, audit trail) plus a dedicated Firebase project (`gdgdeployordie`) and an existing Flutter design system (`aavii_website`) became available. Track A's design is genuinely stronger on the 30%-weighted Agent Engineering and 25%-weighted Architecture judging criteria (two real agents, human approval gate, audit log, citation-only answers), so we're attempting the pivot with Track B kept as an explicit, working fallback if this doesn't finish in time. Full tradeoff reasoning is in the approved plan; the short version:

- **Flutter Web, not native-only**: the source `ARCHITECTURE.md` specified React/PWA. Flutter Web keeps the same Dart codebase (so it's still "the Flutter app"), while staying Playwright-testable and buildable to Android later via the already-registered app.
- **Gemini multimodal for OCR, not Google Cloud Vision**: one call does OCR + structured extraction, reusing the exact Gemini key/model already proven working in Track B — avoids a second untested credential path.
- **A brand-new repo, not `aavii_website` itself**: that repo is a large, unrelated, months-old personal project (a study app) with its own git history and likely-private materials — using it as the hackathon repo would hurt the "clean Git history" story and could force private content public. Only specific reusable files were ported (see below), as fresh files, not a dependency on the old repo.

## Build log

- **Part 1**: `flutter create` (web + android targets) in a fresh repo, `whatswithavi/gdg-slip-flow`. Confirmed web build works before committing. Ported the `vault-ui-soften` Claude Code skill from `aavii_website/.claude/skills/` for consistent UI styling on new screens.
- **Part 2**: Ported the design system — `app_colors.dart`, `app_theme_controller.dart` (dark mode, theme-aware color extension) verbatim; `BrutalistCard`/`BrutalistButton`/`Pressable` verbatim (already in the target soft/rounded style). `BrutalistTextField` was ported but **softened on arrival** — the source file in `aavii_website` was still in the old hard-border/hard-shadow "brutalist" style, so the `vault-ui-soften` rules were applied directly during the port. `AppHeader`/`NavTabBar` were adapted, not copied verbatim: dropped the onboarding-tour hooks (`TourTargets`) and study-app branding/asset dependencies that don't apply here, rebranded, and retargeted the nav to this app's 3 screens (Upload/Approve/Query).
- **Part 3**: Node/Express backend skeleton at `server/`, same shape as Track B's proven `routes/` → `agents/` → `skills/` split, with `dotenv` wired in from the start this time (Track B's first build forgot this and it cost real debugging time).
- **Part 4**: Firebase wiring for both sides.
  - Android package renamed `com.aavii.gdg_slip_flow` → `com.aavii.gdg` to match the Android app already registered in the `gdgdeployordie` Firebase project (confirmed via the `google-services.json` you provided); `MainActivity.kt` moved to match the new package path.
  - `lib/firebase_options.dart` hand-written in the same shape the FlutterFire CLI generates — the CLI's logged-in account (`zenthros.codes@gmail.com`) doesn't have access to `gdgdeployordie` (it was created under a different Google account), so `flutterfire configure` wasn't usable. Built from the Web app config (Firebase Console → Project Settings → Web app) plus `google-services.json`.
  - `server/firebase.ts`: `firebase-admin` initialized with a service account key (`server/firebase-service-account.json`, gitignored, never committed) for server-side Firestore/Storage access. Verified with a real write-then-read-then-delete smoke test against Firestore before wiring it into any route.
  - **Real bug found and worked around**: Firebase's JS interop throws an uncaught `dartException` at runtime specifically under `dart2js` **release** compilation on this Flutter/Dart version (Flutter 3.44.8 / Dart 3.12.2) — confirmed by isolating dependencies one at a time (crash persisted with only `firebase_core` present, no `cloud_firestore`/`firebase_storage`), pinning `firebase_core` to an older stable release (3.15.2 — changed the exception but didn't fix it), and lowering `dart2js` optimization (`--dart2js-optimization=O1` — still crashed). **`flutter run` in debug mode (DDC compiler) and `flutter build web --debug` both work with no crash** — confirmed Firestore and Storage both initialize successfully. Decision: use `flutter build web --debug` as the CI/demo build target. Trade-off accepted: larger, unminified bundle — acceptable for a hackathon submission where "working software" matters more than bundle size, and reversible later if a `dart2js`-compatible plugin version surfaces.
- **Data source note**: `Airtable` was one of the two DB options left open in the original planning doc ("[Airtable/Supabase — pick one]"). Firestore was picked first since the Firebase project was already provisioned and proven working. An Airtable API key was subsequently offered as an additional/alternate data source — how it slots in (replacing Firestore vs. supplementing it, e.g. for an existing reference dataset the QueryAgent should read) will be resolved and documented here once the key and its intended use are provided.

## Time budget

Same checkpoint discipline as the approved plan: if the core flow (upload → extract → approve → query-with-citation) isn't working end-to-end with green CI by roughly the 2/3 mark of remaining time, stop adding scope and fall back to polishing/submitting Track B instead.

- **Part 6**: `SlipExtractionAgent` + `parse_slip_data` skill (see `AGENTS_AND_SKILLS.md` for the full writeup). Verified with a real synthetic test slip image (generated with PIL, not a real photo — but real handwriting-style text) before any UI was built: correctly extracted item/quantity/unit/date/supplier, both Firestore writes (`slips_pending`, `audit_log`) confirmed directly.
  - **Real bug found and fixed**: `server/firebase.ts` read `process.env.GOOGLE_APPLICATION_CREDENTIALS` at module load time, but `dotenv.config()` in `index.ts` runs *after* its imports are evaluated (imports are hoisted in both ES modules and CommonJS) — so `firebase.ts` was reading the environment before `.env` had been loaded. Same root-cause category as Track B's original dotenv bug, different manifestation (transitive import ordering rather than a missing call). Fixed by calling `dotenv.config()` directly inside `firebase.ts` itself, not relying on the entrypoint having done it first.
  - **Firebase Storage: deliberately not enabled — permanent decision, not a pending gap.** Storage returns "the specified bucket does not exist" because Google now requires the Blaze (pay-as-you-go) plan to provision Cloud Storage for a Firebase project at all, even for tiny/free-tier usage — enabling it isn't just a console click, it's opting into a billing plan. Decided against that for a hackathon submission (no card, no billing risk). This was already designed around defensively: the Storage upload step is non-blocking (`uploadRegisterImage`/originally `uploadSlipImage` catches and logs, returns `null` instead of throwing), so extraction, approval, and querying are all fully functional without it — `imageUrl` stays `null` on every record, and the Approval screen already shows a placeholder icon in that case (see `lib/screens/approval_screen.dart`). Documented in `ARCHITECTURE.md` as an accepted trade-off, not a to-do.

- **Part 7**: Upload + Approval screens (Flutter), plus `GET /api/pending-slips`, `GET /api/slips`, `POST /api/approve-slip`, `POST /api/reject-slip`. All Firestore/Storage access stays server-side — the Flutter app only ever calls the backend REST API (`lib/services/api_client.dart`), never touches Firebase directly. This was a deliberate simplification made here (not in the original plan): it means Flutter's `cloud_firestore`/`firebase_storage` packages end up unused by the actual screens (kept for now since Part 4 already proved them working and future native-Android use might want them), removes any need to write Firestore security rules (only the trusted backend, using the admin SDK, ever reads/writes), and keeps a single trusted, audit-logged path for every write.
  - **Real bug found and fixed**: an unhandled promise rejection in `server/routes/slips.ts` (no try/catch) crashed the entire backend process on a malformed request instead of returning an error response. All routes now wrapped; also hit and fixed a Git-Bash-specific `curl` quoting issue during manual testing (inline `-d "{...}"` with escaped quotes got mangled — switched to file-based `--data-binary @file` request bodies for reliable manual testing).
- **Part 8**: `QueryAgent` + `answer_from_records` skill + query screen — the second custom agent/skill pair. Verified with two real cases: a question answerable from one specific record (correctly cited only that record) and a genuinely unanswerable question (correctly refused to guess rather than fabricating an answer).
- **Part 9**: CI/CD. One GitHub Actions job: Node setup → backend lint/build → Flutter setup → `flutter analyze`/`flutter test`/`flutter build web --debug` → Playwright.
  - **Playwright coverage decision**: attempted to activate Flutter Web's accessibility/semantics DOM overlay (the mechanism that would let Playwright's normal click/fill locators drive the canvas-rendered UI) by dispatching pointer events at the `flt-semantics-placeholder` element — a bounded, ~15-minute experiment, not a deep rabbit hole given the earlier dart2js debugging already ate real time this build. It didn't reliably activate. Decision: `tests/app-loads.spec.ts` verifies the real deployed app loads without a JS/console error (genuine regression coverage — this is exactly what would have caught the dart2js crash bug earlier), and `tests/api-flow.spec.ts` drives the actual extract→approve→query and reject flows through the same REST API the UI calls, against the real Gemini API and real Firestore — turning the manual curl verification done throughout this build into permanent, CI-enforced tests. This is real coverage of the agent logic; it just isn't simulated mouse clicks on the canvas.
  - Each CI run's `api-flow.spec.ts` writes one real (correctly-extracted, not synthetic-garbage) record to `slips`/`audit_log` — there's no delete endpoint (out of scope for this build), so a manual Firestore cleanup pass happens before the final demo, not after every CI run.

## 2026-08-08 — Reframing: single-factory tool → configurable business-ops platform

After Parts 1-9 shipped a fully working single-purpose tool (raw-material intake slips only), the product direction broadened: instead of one paper-form type for one factory, build it as a platform any paper-heavy business could configure for its own registers (intake, production, dispatch, attendance, expenses, ...), demoed end-to-end on a real plastic recycling factory. Four pillars: (1) generalized paperwork digitization, (2) workforce automation (attendance + payroll), (3) an owner-facing daily digest agent, (4) a compliance document library. Kept the existing repo/name (`gdg-slip-flow`) — renaming has no functional value and the working git history matters more than the label.

- **Part 10**: Generalized the data model from a hardcoded intake-slip shape to configurable register types (`server/registerTypes.ts`) — a plain config object mapping a register type id (intake/production/dispatch/attendance/expense) to its field schema and a description used in the extraction prompt. Adding a new paper form the system can handle is now a config entry, not new agent/prompt code — this is the extensibility story that makes "usable by different companies" credible rather than just claimed.
  - `SlipExtractionAgent`/`parse_slip_data` → generalized into `RegisterExtractionAgent`/`extract_register_data`: the prompt and output schema are now built dynamically from a `RegisterTypeConfig` instead of being hardcoded to item/quantity/unit/date/supplier. Old files deleted outright (not kept alongside as a compatibility shim) — this is pre-launch code with no external callers.
  - Collections renamed `slips_pending`/`slips` → `records_pending`/`records`; every record now carries `registerType` and a generic `fields: Record<string, string|number|null>` object instead of flat named fields, plus a `companyId` (currently a single hardcoded `demo-factory` constant — see below).
  - `QueryAgent`/`answer_from_records` updated to query `records` and describe itself generically ("a business's digitized paper records... registerType") rather than assuming intake slips; citation field renamed `citedSlipIds` → `citedRecordIds` for consistency.
  - **Multi-tenancy**: every record carries a `companyId` field (`server/registerTypes.ts`'s `DEFAULT_COMPANY_ID`) so the data model is genuinely multi-tenant-ready, but there is no auth or company picker built — full tenant isolation was explicitly scoped out as too large for the remaining time. The architecture supports it; the demo doesn't exercise it.
  - Verified via the same curl-first discipline as every prior part: extract (intake type) → appears in `pending-records` → approve → appears in `records`, all against the real Gemini API and real Firestore, before touching the Flutter side (which is Part 11 — the app still calls the old `*-slip` endpoints until then and will not build against the backend until that part lands).
  - Query itself hit the pre-existing Gemini free-tier daily quota wall (20 requests/day on the current model) during this verification pass — not a regression from this change; extraction and the Firestore read/write path are unaffected.
- **Part 11**: Updated the Flutter side to match — `ApiClient` now calls the generalized `*-record` endpoints and adds `fetchRegisterTypes()`. `HomeShell` (`main.dart`) fetches register types once at startup and passes them down to `UploadScreen`/`ApprovalScreen` rather than each screen re-fetching or hardcoding a field list.
  - `UploadScreen` gained a register-type picker (pill chips) above the camera/gallery buttons; the extraction result card renders whichever fields the selected type defines, read from the type config, instead of hardcoded item/quantity/unit.
  - `ApprovalScreen`'s pending-record card builds its `TextEditingController`s dynamically from the matching register type's field list (a `Map<String, TextEditingController>` keyed by field key, not five hardcoded named controllers) — this is what makes one card widget work for intake, production, dispatch, attendance, and expense records without an if/else per type.
  - Verified end-to-end through the real running app (not just curl): loaded the built Flutter Web app against the real backend, confirmed `GET /api/register-types` succeeds and the app renders past its loading state. Full click-driven UI verification isn't reliable here (same Flutter-Web-canvas limitation as Part 9), so this pairs with `tests/api-flow.spec.ts` (updated to the new endpoint/field shapes) as the actual regression coverage of the underlying logic.
- **Part 12**: Payroll calculation (`server/services/payroll.ts`, `GET /api/payroll?wageRate=...`). Deliberately **not** an LLM skill — built as plain deterministic code (sum hours per worker from approved `attendance` records, multiply by the given rate) and kept in `server/services/` rather than `server/skills/` to keep that distinction real in the codebase, not just asserted in docs. Attendance itself needed no new extraction work — it was already a register type from Part 10, and `QueryAgent` already answers workforce questions generically since it wasn't hardcoded to intake-slip semantics.
  - Verified against seeded attendance data written directly to Firestore rather than through real extraction — this is a deterministic-math test, so spending Gemini quota (already tight, see Part 10) to generate a test image would have been pure waste. Confirmed correct per-worker hour totals and wage math, and that an "absent" day with 0 hours correctly doesn't count toward days-present.
- **Part 13**: `DailyDigestAgent` + `summarize_activity` skill — the **third** custom agent, and the first that's proactive rather than reactive (it summarizes activity unprompted, instead of responding to a specific upload or question). `GET /api/digest` gathers recent approved-record counts by type, low-confidence pending items, and audit-log activity tallies over a 7-day window, then asks Gemini to turn that into a short owner-facing summary + highlights.
  - Verified with a single real Gemini call (quota discipline — see below): the returned summary correctly matched the actual accumulated counts from this build's own testing (6 extractions, 5 approvals, 1 rejection, 4 queries, 2 intake + 4 attendance approved), and correctly reported zero low-confidence items rather than inventing any.
  - **Quota note**: hit the same `RESOURCE_EXHAUSTED` error as Part 10's query test, but a retry ~10s later (the delay the error response itself suggested) succeeded — despite the error's `quotaId` being labeled `GenerateRequestsPerDayPerProjectPerModel-FreeTier`, in practice it clears faster than "daily" implies. Useful for the remaining build: a failed Gemini call here is often worth one retry after the suggested `retryDelay`, not an immediate hard stop.
- **Part 14**: Compliance document library. `POST/GET /api/compliance-docs` — text-in (title + content + optional expiry date), not PDF upload/parsing; that's a real scope expansion (extraction, OCR-for-scanned-PDFs) not worth the remaining time, and pasting the relevant text achieves the same "ask questions with citations against your own compliance docs" goal. `expiryDate` is uploader-provided, not LLM-extracted, to avoid spending more of the tight Gemini quota on a field a human filling out a form can just type. `QueryAgent`/`answer_from_records` extended to fetch and cite compliance docs alongside operational records (`citedDocIds` alongside `citedRecordIds`) — see `AGENTS_AND_SKILLS.md`.
  - Flutter side: new `InsightsScreen` (4th nav tab) combining the digest agent's UI and the compliance-doc upload form/list — deliberately one tab, not two, since each has little enough content that a 5-tab bar wasn't justified. `QueryScreen` updated to show both citation types.
  - **Verification gap, disclosed honestly**: the doc-upload endpoint itself was verified directly (curl round-trip confirmed a real write/read). The *query-citing-a-compliance-doc* path could not be live-verified this session — two attempts both hit `RESOURCE_EXHAUSTED` with a growing `retryDelay` (1s, then 31s), unlike Part 13's transient hit, indicating the actual daily cap (20 requests/day, free tier) is now genuinely exhausted, not a burst limit. The code follows the exact same request/response contract already proven working for records-only citation (Parts 8, 10), so confidence is reasonably high, but this specific path needs a live re-check once quota resets or a second API key is available — flagged to the user directly, not silently assumed correct.
  - **Real bug found and fixed while writing the widget test**: `test/widget_test.dart` still asserted on hardcoded old-era screen text ("Upload a slip", 3 nav tabs) and had gone stale across Parts 11-14 without being updated alongside the screens it was testing. Running it (not just `flutter analyze`, which doesn't execute tests) surfaced a genuine failure: `HomeShell`'s register-types loading gate (added in Part 11) means Flutter's `TestWidgetsFlutterBinding` — which forces every HTTP request to fail with a 400 — leaves the app permanently on its error state in a widget test, so screen-content assertions can never pass here. Rewrote the test to assert what's actually reachable without a live backend (graceful error display, nav bar renders) and documented why deeper functional coverage belongs in `tests/api-flow.spec.ts`/`tests/app-loads.spec.ts` instead. Lesson for the rest of this build: `flutter analyze` passing is not evidence a test still passes — run `flutter test` too, not just after touching test files directly.

- **Part 15**: Groq as an automatic fallback LLM provider (`server/llm.ts`). All three agents previously duplicated near-identical "fetch the Gemini API" boilerplate; centralized it into `callLLMText`/`callLLMVision`, which try Gemini first and fall back to Groq **specifically on a 429 quota error** (`qwen/qwen3.6-27b` for vision, `openai/gpt-oss-20b` for text) — not on other failure modes, so a real bug (malformed request, bad prompt) still fails loudly instead of silently retrying against a different provider and masking it. This is the exact multi-provider resilience pattern the hackathon brief itself recommends (switch providers on rate limits rather than blocking on one), now actually built rather than just planned.
  - Groq's request/response format is a different (OpenAI-compatible chat completions) shape from Gemini's — confirmed the exact model IDs and request format via Groq's docs before writing code, rather than guessing.
  - **Real bug found and fixed**: Groq's Qwen model runs in "thinking mode" by default, prepending a `<think>...</think>` reasoning block before the actual JSON answer — Gemini never does this, and the first vision-fallback test failed to parse as JSON because of it. Fixed by stripping the block centrally in `llm.ts` (`stripThinkingBlock`), not in every skill's parser, since it's a provider quirk the skills shouldn't need to know about.
  - **Verified for real, immediately**: Gemini's quota was still exhausted at the time this was tested, so every call in this verification pass — vision extraction, text query (including the compliance-doc citation path left unverified in Part 14), and the full `api-flow.spec.ts` Playwright suite — genuinely exercised the Groq fallback end-to-end, not a mocked/simulated path. Both tests passed.
- **Part 16**: CI had been red since the register-types generalization began (every commit from Part 10 onward) — checked the actual failure logs rather than assuming, and confirmed it was purely the Gemini free-tier quota (20 requests/day) being exhausted by repeated CI runs each spending real quota, not a code regression (the third test, `app-loads.spec.ts`, passed on every one of those "failing" runs). Added `GROQ_API_KEY` as a GitHub Actions repo secret and passed it through to the Playwright step. **CI is now green** — full run: backend lint/build, `flutter analyze`/`flutter test`/`flutter build web --debug`, and the complete Playwright suite, all passing.

## 2026-08-08 — On-device testing detour (Track B archived, backend hosting attempted and abandoned)

- Archived (not deleted — reversible) `whatswithavi/gdg_deploy_or_die` (Track B). Slip Flow is the sole hackathon submission from this point on.
- Rebuilt and sent a debug APK reflecting all pillars through Part 16. Confirmed it fails cleanly with a readable "connection refused to localhost:3000" error on a real device — expected, since the app is hardcoded to `localhost` and a phone's localhost isn't this PC.
- Tried wireless ADB debugging as a fix (no USB cable available) — abandoned after repeated difficulty locating Developer Options/Wireless debugging on the user's specific phone; this was costing more time than the on-device test was worth.
- Tried deploying the backend to Vercel instead (would fix on-device access permanently, not just for this one test) — the connected Vercel account returned `403 forbidden` on project creation, and the only existing project in that account is an unrelated site (`website`) not to be touched. Prepared the backend for serverless deployment along the way (see below) even though the Vercel attempt itself didn't complete — that work isn't wasted, it's needed for whichever host is used next.
  - `server/firebase.ts` extended to accept the service account as a raw JSON string via `FIREBASE_SERVICE_ACCOUNT_JSON` (parsed directly, no file write), not just a file path — serverless hosts have no persistent filesystem to point `GOOGLE_APPLICATION_CREDENTIALS` at. Verified against real Firestore before considering it done.
  - `server/index.ts` gained `export default app` (Vercel's documented Express pattern) alongside the existing named export.
  - Found and cleaned up a real mess: `server/dist/` had accumulated orphaned compiled `.js` files from source deleted back in Part 10 (`slipExtractionAgent.js`, `extractSlip.js`, `slips.js`, `parse_slip_data.js`) — `tsc` doesn't delete output for removed source files. A `rm -rf dist && npm run build` now needs to happen before any deploy, not just `npm run build`.
- **Decision**: paused the hosting/on-device-testing thread rather than sinking more time into it. The Flutter Web build is fully tested and working; on-device Android testing remains a nice-to-have, not a blocker, until a hosting option that actually has permissions is chosen.

## 2026-08-08 — Face-recognition attendance check-in (Part 17)

New capability, not part of the original 4-pillar scope: photo-based attendance ("look at the camera" instead of filling a paper sheet), requested directly by the user. Scoped deliberately after flagging two tradeoffs upfront and getting explicit confirmation to proceed anyway:
- **Reliability**: this compares photos using a general-purpose vision LLM (`callLLMMultiVision`, Gemini-first/Groq-fallback, same as everything else), not a dedicated face-recognition embeddings model. Meaningfully less rigorous than purpose-built biometric matching — fine for a small enrolled team in a demo, not a production biometric-accuracy claim.
- **Privacy**: worker face photos are biometric data, which carries real consent/data-handling weight in many jurisdictions. Flagged explicitly before building, not treated as a routine technical add-on.

**What was built**: `FaceAttendanceAgent` (4th custom agent) + `match_worker_face` (4th skill) — see `AGENTS_AND_SKILLS.md` for full detail. `server/llm.ts` extended with `callLLMMultiVision`/`callGeminiMultiVision`/`callGroqMultiVision` to send N reference photos + 1 new photo in a single request (Groq's OpenAI-compatible format already supports multiple `image_url` content parts, so this extended cleanly). New `workers` collection stores enrolled reference photos as base64 *inside Firestore documents* — Storage still isn't enabled (see Part 6/Open risks), so this is the same workaround pattern, applied to a new kind of image. A successful match writes to `records_pending` with `registerType: "attendance"` — deliberately **not** an auto-approve bypass; it goes through the exact same human-approval screen as every other register type, and because it's just another attendance-type record, the existing dynamic Approval-screen card renders it correctly with zero UI changes needed there.

**Verified** with two synthetic test images (generated cartoon "faces" with clearly different colors/features, not real photos — avoids needing actual biometric data just to exercise the code): correctly matched a photo against its own enrollment while distinguishing it from a second enrolled worker (confidence 1.0, and the `reasoning` field cited the actual distinguishing features), and correctly returned no match (confidence 0, no record created) for a third, unenrolled test image rather than guessing. This is real evidence the multi-image comparison and confidence-gating logic work correctly — it is not evidence of real-world face-recognition accuracy on actual human photos, which hasn't been tested and shouldn't be assumed.

**Part 18** (Flutter side): new `FaceAttendanceScreen`, a 5th nav tab ("Check-in"). Two sections: enroll a worker (name + reference photo captured via `image_picker`, compressed client-side — `maxWidth: 512, imageQuality: 70` — since photos are stored directly as base64 in Firestore documents, not Storage, so keeping them small matters more than it would with real object storage) and check-in (capture a photo, submit, see the match result with confidence and whether an attendance record was created). Reused `ApiClient`'s existing error-surfacing pattern rather than inventing a new one.
  - Widget test updated for the 5th tab; verified through the real running app (register-types loads, no console errors, renders past loading state) — same verification approach as every other UI addition this build, given Flutter Web's canvas isn't reliably click-automatable here.

## 2026-08-08 — Digital receipt emails on approval (Part 19)

`server/services/email.ts`: on every record approval, format the approved fields into an HTML receipt and email it via SendGrid — the originally-deferred "email backup" idea from `ARCHITECTURE.md` section 9, now built at the user's request, scoped to all register types (not just one) with a single fixed recipient. Deliberately plain code, not an LLM skill, same reasoning as `calculatePayroll`: formatting known structured fields into HTML doesn't benefit from a language model.

- **Non-blocking by design**, same pattern as Storage image uploads: a failed send is caught, logged, and does not fail the approval — the approval itself is already durably written to Firestore before the email is even attempted. `receipt_emailed` is logged to `audit_log` only on success, so the audit trail itself reveals whether a given approval's receipt actually went out.
- **A real hard requirement surfaced immediately on first test**: SendGrid rejects sending from any address that isn't verified as a sender identity, even with a valid API key — this isn't a bug, it's how SendGrid works, and it was flagged to the user *before* building (see the AskUserQuestion exchange), not discovered as a surprise after. Confirmed via a real approval against the real SendGrid API: the approval succeeded and the non-blocking error handling worked exactly as designed, with the actual SendGrid 403 (`sender identity not verified`) surfaced in the server log rather than silently swallowed. Needs the user to complete sender verification in the SendGrid dashboard (Settings → Sender Authentication → Verify a Single Sender) before receipts will actually deliver — a one-time account-side step, not something fixable in code.

## 2026-08-08 — Part H: final polish

- Fixed a test flakiness bug surfaced by the last CI run (see the "Fix flaky query test" commit) before doing anything else — verifying nothing was actually broken took priority over cosmetic polish.
- Full verification sweep: backend lint/build, `flutter analyze`, `flutter test`, and a live smoke test of every GET endpoint (`register-types`, `pending-records`, `records`, `compliance-docs`, `workers`, `payroll`) — all clean.
- **Cleared all accumulated test data from the live Firestore project** (18 approved records, 2 pending, 1 compliance doc, 2 enrolled test workers, 76 audit_log entries) — all of it was synthetic data generated during this build's own testing (the two enrolled "workers," for instance, were the cartoon test images from Part 17/18), not anything real. Done via a one-off script, not a permanent feature — there's still no delete endpoint in the app itself (unchanged design decision, see earlier parts). Gives a clean slate for the real demo instead of a cluttered dataset with a dozen near-duplicate test intake slips.
- `README.md` rewritten — it had described only the original single-agent, intake-only scope since Part 5 and hadn't been touched since, badly understating what's actually built by this point (4 agents, 4 skills, 2 deterministic services, 5 register types, face check-in, email receipts).

## 2026-08-08 — SendGrid sender verification resolved

- User completed SendGrid's Single Sender Verification, but for `whatswithavi@gmail.com`, not `zenthros.codes@gmail.com` (the address originally configured as `RECEIPT_SENDER_EMAIL`) — a re-test still failed with the same 403 error afterward, which looked like the fix hadn't worked. Rather than guess, queried SendGrid's `/v3/verified_senders` API directly with the real key, which showed exactly one verified sender: `whatswithavi@gmail.com`. Confirmed by a screenshot of the SendGrid dashboard's Single Sender Verification page. Root cause was a mismatched address, not a broken verification.
- Fixed by updating `RECEIPT_SENDER_EMAIL` (local `.env` only — this is a per-environment secret, never committed) to the actually-verified address; `RECEIPT_RECIPIENT_EMAIL` stays `zenthros.codes@gmail.com`, since SendGrid only requires the *sender* to be verified, not the recipient.
- **Real receipt email confirmed sent**: a live approval wrote `receipt_emailed` to `audit_log` for that record — the first attempt after the fix hit a transient `fetch failed` network error (unrelated to SendGrid/sender config, didn't recur on retry), the second attempt succeeded cleanly. This is genuine evidence of a successful send, not just "no error thrown" — checked `audit_log` directly rather than trusting an absence of console output.
- Test records created during this verification pass cleaned up the same way as the Part H sweep.

## Open risks

- The `dart2js` release-mode Firebase crash (see Part 4) — mitigated by using `--debug` builds, but worth re-testing if Flutter/Firebase plugin versions change.
- Firebase CLI on this machine can't directly manage the `gdgdeployordie` project (different Google account) — any future FlutterFire CLI regeneration needs to happen from the account that owns the project, or config needs to keep being hand-maintained.
- Firebase Storage is deliberately not enabled (Blaze-plan requirement — see Part 6). Not a risk to fix, a permanent scope decision: no record images will display (placeholder icon instead), everything else is unaffected.
